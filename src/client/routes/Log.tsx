import { Placeholder } from "../components/Placeholder";

export function Log() {
  return (
    <Placeholder
      eyebrow="Log"
      title="What did you eat?"
      note="Camera, barcode, or a sentence — AI returns items with confidence, you confirm or tweak, it saves. Happy path under ten seconds."
      lands="QUICK-ADD + CONFIRM SHEET — M2 · #9, #10"
    />
  );
}
