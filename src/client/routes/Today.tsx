import { Placeholder } from "../components/Placeholder";

export function Today() {
  return (
    <Placeholder
      eyebrow="Today"
      title="The daily budget"
      note="Remaining kcal, the budget meter with its earned extension, macros, and the meal + run timeline."
      lands="BUDGET RING + TIMELINE — M2 · #11"
    />
  );
}
